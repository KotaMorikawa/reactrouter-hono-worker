{
  "name": "@repo/db",
  "version": "1.0.0",
  "private": true,
  "description": "Database schema and migrations with Drizzle ORM",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "biome check src/",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.29.0",
    "postgres": "^3.4.3"
  },
  "devDependencies": {
    "drizzle-kit": "^0.20.0",
    "typescript": "^5.8.3",
    "@biomejs/biome": "^1.8.0",
    "vitest": "^1.6.0"
  }
}
